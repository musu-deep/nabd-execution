{
  "name": "nabd-execution-mobile",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare:index": "node scripts/reconstruct-index.mjs",
    "predev": "npm run prepare:index",
    "dev": "vite",
    "prebuild": "npm run prepare:index",
    "build": "vite build",
    "prepreview": "npm run prepare:index",
    "preview": "vite preview",
    "android:add": "npm run build && npx cap add android",
    "android:sync": "npm run build && npx cap sync android",
    "android:open": "npx cap open android",
    "ios:add": "npm run build && npx cap add ios",
    "ios:sync": "npm run build && npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0",
    "@capacitor/share": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/assets": "^3.0.0",
    "vite": "^7.0.0"
  }
}
